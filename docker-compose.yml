version: "3.8"

services:
  frontend:
    image: wardrobe-app-fe
    container_name: wardrobe-app-fe
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8201:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: always
